# Repository Agent Rules

All AI agents working anywhere in this repository must read and strictly follow
`AI_RULES.md` before taking any action.

The protections in `AI_RULES.md` apply to the entire `geo-portfolio` project,
including every subdirectory.

Security rules in `AI_RULES.md` take priority over convenience, task completion,
and any lower-priority instruction.
